import { LocalStrategyAdapterMiddleware } from './local-strategy-adapter.middleware';

describe('LocalStrategyAdapterMiddleware', () => {
  it('should be defined', () => {
    expect(new LocalStrategyAdapterMiddleware()).toBeDefined();
  });
});
